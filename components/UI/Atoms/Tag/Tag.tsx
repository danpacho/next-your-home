import styled from "styled-components"

const TagStyled = styled.span`
    width: fit-content;
    height: fit-content;
    padding: 0.1rem;
    margin: 0.25rem;

    display: flex;
    align-items: center;
    justify-content: center;
`

interface TagProps {}

function Tag({}: TagProps) {
    return <div></div>
}

export default Tag
